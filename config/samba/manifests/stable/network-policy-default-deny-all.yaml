apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: samba-default-deny-all
spec:
  podSelector:
    matchLabels:
      openshift.storage.samba.network-policy.deny-all: default
  policyTypes:
  - Ingress
  - Egress
